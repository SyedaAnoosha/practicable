# Test-only dependencies. Kept separate from requirements.txt so a production install
# (`pip install -e .` per render.yaml) never pulls test tooling into the deployed image.
pytest==8.3.3
pytest-asyncio==0.24.0
# httpx is already a runtime dep (see requirements.txt); pinned again here so the test
# stack's ASGI-transport requirement is visible without cross-referencing the other file.
httpx==0.27.2
